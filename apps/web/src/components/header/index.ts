/**
 * Header component and types.
 * Subcomponents (CollectionChip, NewCollectionPopover) are internal to the header folder.
 */

export type { HeaderCollection } from "./header";
export { Header } from "./header";

import { Header } from "./header";
export default Header;
